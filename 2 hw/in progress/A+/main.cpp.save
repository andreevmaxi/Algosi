#include <iostream>

struct Heap{
  std::vector<int> values;

  void SiftUp(int i){
    if(i != 0){
      while(values[(i - 1)/2] > values[i]){
        std::swap(values[(i - 1)/2], values[i]);
      }
    }
    return;
  }

  void SiftDown(int i){
    if(i != 0){
      while(values[i] > values[i * 2 + 1] || values[i] > values[i * 2 + 2]){
        std::swap(values[i], values[i * 2 + 1]);
        int f = values[i];
        int s = values[
      }
    }
  }
};

int main()
{
  int n;

  std::cin >> n;

  int ans = n;
  return 0;
}
