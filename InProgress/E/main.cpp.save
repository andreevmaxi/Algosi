#include <iostream>
#include <cstdlib>

unsigned int a, b;
unsigned int cur = 0;

unsigned int nextRand() {
  cur = cur * a + b;
  return cur >> 8;
}


int main(){
  int m,q;
  std::cin >> m >> q;
  std::cin >> a >> b;
  pair<int, int> gym[m*2+2*q];
  for(int i=0; i<m; ++i){
    int add = nextRand();
    int l = nextRand();
    int r = nextRand();
    if(l > r) swap(l, r);
    
  }
  return 0;
}
