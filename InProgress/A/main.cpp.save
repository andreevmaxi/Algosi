#include <iostream>
#include <stack>
#include <fstream>

int main(){
  int N;
  std::ifstream in("inverse.in");
  in >> N;

  pair<int, int> a[N];

  for(int i = 0; i<N; ++i){
    in >> a[i].first();
    a[i].second() = i;
  }
  in.close();
  sort(a, N);
  
  for(int i =)

  std::ofstream out("inverse.out");
  out << Answer;
  out.close();
  return 0;
}
